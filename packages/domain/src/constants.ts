/**
 * Default product name. A single configurable value: apps resolve
 * `process.env.APP_NAME ?? DEFAULT_APP_NAME`, so the brand can change
 * project-wide without touching the domain.
 */
export const DEFAULT_APP_NAME = 'Fides';
